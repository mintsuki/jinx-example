name=python
version=3.8.13
source_method=git
clone_url="https://github.com/python/cpython.git"
tag="v${version}"
hostdeps="autoconf automake libtool pkg-config autoconf-archive"

regenerate() {
    autoreconf -f -i
    cp /usr/local/share/automake-1.16/config.sub ./
}
