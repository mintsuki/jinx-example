name=limine
from_source=limine
revision=1
hostdeps="gcc"
imagedeps="base-devel nasm"

configure() {
    "${source_dir}"/configure CFLAGS="${common_cflags}" CXXFLAGS="${common_cflags}" --prefix="${prefix}" TOOLCHAIN=x86_64-vinix
}

build() {
    make -j${parallelism}
}

install() {
    DESTDIR="${dest_dir}" make install-strip
}
