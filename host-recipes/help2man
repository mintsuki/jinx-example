name=help2man
from_source=help2man
revision=1

configure() {
    "${source_dir}"/configure --prefix="${prefix}"
}

build() {
    make -j${parallelism}
}

install() {
    make install DESTDIR="${dest_dir}"
}
